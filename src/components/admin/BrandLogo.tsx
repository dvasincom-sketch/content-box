import React from 'react'

/**
 * Логотип «Контент Бокс» для сайдбара админки — только текст.
 */
export default function BrandLogo() {
  return (
    <span style={{ fontSize: 19, fontWeight: 700, color: 'var(--theme-text)', letterSpacing: '0.01em', fontFamily: 'Play, sans-serif' }}>
      Контент Бокс
    </span>
  )
}
